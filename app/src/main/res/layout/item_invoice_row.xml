<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="wrap_content"
    android:orientation="vertical" android:padding="4dp">
    <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content"
        android:orientation="horizontal" android:gravity="center_vertical" android:paddingBottom="2dp">
        <TextView android:id="@+id/tvItemNumber"
            android:layout_width="22dp" android:layout_height="22dp"
            android:background="@color/primary" android:gravity="center"
            android:text="1" android:textColor="@color/white"
            android:textSize="10sp" android:textStyle="bold"/>
        <EditText android:id="@+id/etItemName"
            android:layout_width="0dp" android:layout_height="42dp" android:layout_weight="1"
            android:layout_marginHorizontal="4dp" android:hint="Item name"
            android:textSize="12sp" android:paddingHorizontal="8dp"
            android:background="@drawable/bg_input_item" android:inputType="textCapSentences"/>
        <ImageButton android:id="@+id/btnRemoveItem"
            android:layout_width="32dp" android:layout_height="32dp"
            android:src="@android:drawable/ic_delete"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:tint="@color/error"/>
    </LinearLayout>
    <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content"
        android:orientation="horizontal" android:paddingStart="26dp">
        <EditText android:id="@+id/etQuantity"
            android:layout_width="0dp" android:layout_height="38dp" android:layout_weight="0.8"
            android:layout_marginEnd="3dp" android:hint="Qty" android:textSize="11sp"
            android:paddingHorizontal="6dp" android:background="@drawable/bg_input_item"
            android:inputType="numberDecimal" android:gravity="center"/>
        <EditText android:id="@+id/etUnitPrice"
            android:layout_width="0dp" android:layout_height="38dp" android:layout_weight="1.2"
            android:layout_marginEnd="3dp" android:hint="₹ Price" android:textSize="11sp"
            android:paddingHorizontal="6dp" android:background="@drawable/bg_input_item"
            android:inputType="numberDecimal"/>
        <EditText android:id="@+id/etDiscount"
            android:layout_width="0dp" android:layout_height="38dp" android:layout_weight="0.8"
            android:layout_marginEnd="3dp" android:hint="Disc%" android:textSize="11sp"
            android:paddingHorizontal="4dp" android:background="@drawable/bg_input_item"
            android:inputType="numberDecimal"/>
        <EditText android:id="@+id/etTax"
            android:layout_width="0dp" android:layout_height="38dp" android:layout_weight="0.8"
            android:layout_marginEnd="3dp" android:hint="Tax%" android:textSize="11sp"
            android:paddingHorizontal="4dp" android:background="@drawable/bg_input_item"
            android:inputType="numberDecimal"/>
        <TextView android:id="@+id/tvLineTotal"
            android:layout_width="0dp" android:layout_height="38dp" android:layout_weight="1.2"
            android:background="@color/surface_variant" android:gravity="center|end"
            android:paddingEnd="6dp" android:text="₹0.00" android:textColor="@color/primary"
            android:textSize="11sp" android:textStyle="bold"/>
    </LinearLayout>
    <View android:layout_width="match_parent" android:layout_height="1dp"
        android:layout_marginTop="6dp" android:background="@color/divider"/>
</LinearLayout>
